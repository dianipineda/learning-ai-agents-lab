ANTHROPIC_API_KEY=
ANTHROPIC_WORKSPACE_ID=
# Opcionales (valores por defecto en agent_harness/config.py)
# AGENT_MODEL=claude-sonnet-5
# AGENT_PRESUPUESTO_TOKENS=12000
# AGENT_VENTANA_MENSAJES=40
# AGENT_CHECKPOINT_DB=agent.db      # ruta SQLite = memoria que sobrevive reinicios
# AGENT_LOG_LEVEL=INFO             # WARNING por defecto; INFO = un JSON por evento, por thread_id
